[general]
# stdout_logfile=""
# stderr_logfile=""

disabled_units = [
    "a",
    "b"
]
copy_command="wl-copy '{text_to_copy}'"
# TODO: dont forget the ''!!!

[general.display]
# show_tags = false 
# show_ids = false
show_prefixes = true 

menu_prompt = "quick actions"

# label_style = "{label}"
# id_style = "{id}"
# prefix_style = "{prefix}"
# tag_style = "{tag}"
# tag_separator = " "


[general.launcher]
# -------wofi
# launcher_command="wofi --prompt='{prompt}' --dmenu --width=70% --height=60%"

# -------rofi
launcher_command="rofi -dmenu -case-smart -markup-rows -p '{prompt}'"

[general.persistance]
backend = "pickle"
pickle_location = "/home/leswellhm/extra.pickle"